language: scala
scala:
  - 2.11.8

sudo: false

script: sbt "+ test" -Dloglevel=debug

jdk:
 - oraclejdk8
 - oraclejdk7

branches:
  only:
    - develop

addons:
  apt:
    packages:
      - oracle-java8-installer
